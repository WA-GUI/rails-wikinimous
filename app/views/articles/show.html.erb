<br>
<h1><%= @article.title %></h1>
<p><%= @article.content %></p>

<%= link_to "Edit article", edit_article_path(@article)%>
<%= link_to  "Erase article", article_path(@article), method: :delete, data: {confirm: "Are you sure ? "}%>
<%= link_to  "Return to articles", articles_path(@article)%>
